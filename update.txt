### SYSTEM ROLE & CONTEXT
You are an expert full-stack developer specializing in lightweight, practical web applications for MSMEs. We are building the "Gamas" app. I want to implement two main features in the backend/frontend: a system for managing Settings & Backups, and an Automated WhatsApp Reminder for customer credit/receivables (tempo barang).

Keep the code clean, modular, and easy to maintain.

---

### FEATURE 1: SETTINGS & BACKUP MANAGEMENT

1. **UI/UX Requirement:**
   - Create a clean interface for the "Pengaturan & Backup" section.
   - Include a "Backup Data" button and a "Restore Data" file input.
2. **Functional Requirement:**
   - **Backup:** When the user clicks "Backup Data", export the local application data (e.g., SQLite database, local storage, or JSON file depending on our stack) into a downloadable `.json` or `.sql` file.
   - **Restore:** Allow the user to upload the backup file to restore the application state. Add a confirmation prompt before overwriting existing data.

---

### FEATURE 2: AUTOMATED WHATSAPP RECEIVABLE REMINDER (TEMPO BARANG)

1. **Data Structure Update:**
   - Ensure the Customer/Order data model includes these fields:
     * `customer_phone`: String (WhatsApp format, e.g., 628xxx)
     * `due_date` (Tanggal Jatuh Tempo): Date
     * `payment_status`: String ('Lunas' / 'Belum Bayar')
     * `last_reminder_sent`: Date/Nullable

2. **Core Logic (The "3-Day Grace Period" Rule):**
   - Create a background function or an automated check that filters for accounts where:
     * `payment_status` == 'Belum Bayar'
     * The current date is exactly **3 days after** the `due_date` (Current Date - Due Date = 3 Days).
   - If a customer matches this condition, trigger a WhatsApp reminder action.

3. **WhatsApp Integration Approach:**
   - Provide a practical, low-cost solution for a WhatsApp system:
     * **Option A (Manual Click-to-Chat):** Generate a list of "Overdue Reminders" in the admin dashboard with a "Kirim WA" button. Clicking this button opens a WhatsApp Web link (`https://wa.me/phone?text=urlencoded_text`) with a pre-filled template so the admin can send it manually.
     * **Option B (Automated Background Script):** Sketch a lightweight backend cron-job template/function that could later connect to a WhatsApp API Gateway (like Baileys, FonePace, or similar webhooks).
   *Please provide Option A first for immediate use, and comment out Option B for future scaling.*

4. **Message Template (Indonesian):**
   "Halo Kak [Nama Customer], ini dari Gamas. Mengingatkan kembali untuk nota belanjaan tanggal [Tanggal Nota] yang jatuh tempo pada [Tanggal Jatuh Tempo] sebesar [Total Tagihan] saat ini belum tercatat lunas. Mohon segera melakukan penyelesaian pembayaran ya Kak. Terima kasih banyak 🙏"

---

### OUTPUT EXPECTED
1. Code implementation for the Settings & Backup UI and its handler functions.
2. Code implementation for the Due Date filtering logic and the WhatsApp URL generator/sender function.
3. Brief explanation of where to paste or integrate this code into the existing structure.